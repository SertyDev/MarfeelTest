import "jasmine";
import {initialize} from '../src/app';

describe('Initialize Unit Test', () => {
  
    // it('should return the resolve value', () => {
    //   expect(initialize).toBe('resolve value');
    // });

  });
